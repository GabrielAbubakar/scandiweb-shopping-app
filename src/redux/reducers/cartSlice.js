const initialState = {
    products: [],
    cart: [],
    currency: { unit: "USD", symbol: "$" },
    category: "clothes"
}

const cartSlice = (state = initialState, action) => {
    switch (action.type) {






















        default:
            return state
    }
}

export default cartSlice